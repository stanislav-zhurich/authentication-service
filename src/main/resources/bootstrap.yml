spring:
  application:
    name: authentication-service
  profiles:
    active: default
server:
   port: 9003
security:
  oauth2:
    client:
      client-id: test
      client-secret: test
      scope: user
logging:
  level:
    org.springframework.security: DEBUG
